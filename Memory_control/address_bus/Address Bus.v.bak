//address bus, part of memory control 

module Address_bus(SR1, pc, LDR, STR, add_bus);
input [31:0] SR1;
input pc,LDR, STR;
output reg add_bus;

always@*
begin
if(LDR==1|STR==1)
add_bus=SR1;
else 
add_bus=pc;
end 
endmodule 

